<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mot de passe modifié - <%= @app_name %></title>
</head>
<body>
  <div style="<%= email_wrapper_style %>">
    <div style="<%= email_container_style %>">

      <!-- Header Section -->
      <div style="<%= email_header_style('success') %>">
        <div style="<%= email_logo_style %>">✅</div>
        <h1 style="<%= email_header_title_style %>">Mot de passe modifié</h1>
        <p style="<%= email_header_subtitle_style %>">Votre compte <%= @app_name %> est sécurisé</p>
      </div>

      <!-- Main Content -->
      <div style="<%= email_content_style %>">
        <h2 style="<%= email_content_title_style %>">
          Bonjour <%= @user.first_name || @user.email.split('@').first %>,
        </h2>

        <p style="<%= email_content_text_style %>">
          <strong>Votre mot de passe a été modifié avec succès !</strong>
        </p>

        <p style="<%= email_content_text_style %>">
          Cette modification a été effectuée le
          <strong><%= Time.current.strftime("%d/%m/%Y à %H:%M") %></strong>
          depuis votre compte <%= @app_name %>.
        </p>

        <!-- Success Confirmation Box -->
        <div style="<%= email_info_box_style('success') %>">
          <h4 style="margin: 0 0 10px 0; font-size: 18px;">
            🎉 Changement confirmé
          </h4>
          <p style="margin: 0;">
            Votre nouveau mot de passe est maintenant actif.
            Vous pouvez l'utiliser dès maintenant pour vous connecter.
          </p>
        </div>

        <!-- Action Button -->
        <div style="text-align: center; margin: 30px 0;">
          <a href="<%= new_user_session_url %>" style="<%= email_button_style('success') %>">
            Se connecter maintenant 🔐
          </a>
        </div>

        <!-- Security Alert Box -->
        <div style="<%= email_info_box_style('warning') %>">
          <h4 style="margin: 0 0 10px 0; font-size: 18px;">
            ⚠️ Ce n'était pas vous ?
          </h4>
          <p style="margin: 0;">
            Si vous n'avez pas modifié votre mot de passe, contactez immédiatement notre équipe sécurité à
            <a href="mailto:<%= @support_email %>" style="color: #856404; font-weight: bold;">
              <%= @support_email %>
            </a>
          </p>
        </div>

        <!-- Security Tips -->
        <div style="<%= email_security_notice_style %>">
          <h4 style="<%= email_security_title_style %>">
            💡 Conseils de sécurité
          </h4>
          <p style="<%= email_security_text_style %>">
            Pour protéger votre compte :
          </p>
          <ul style="<%= email_security_text_style %>; margin: 10px 0 0 0; padding-left: 20px;">
            <li>Ne partagez jamais votre mot de passe</li>
            <li>Déconnectez-vous sur les appareils partagés</li>
            <li>Surveillez l'activité de votre compte</li>
          </ul>
        </div>

        <p style="<%= email_content_text_style %>">
          Merci de faire confiance à <%= @app_name %> pour vos achats de photos de chats adorables ! 🐱
        </p>

        <p style="<%= email_content_text_style %>">
          Sécurisement vôtre,<br>
          <strong>L'équipe <%= @app_name %></strong>
        </p>
      </div>

      <!-- Footer -->
      <div style="<%= email_footer_style %>">
        <p style="<%= email_footer_text_style %>">
          <strong><%= @app_name %></strong> - Sécurité et confidentialité
        </p>
        <p style="<%= email_footer_text_style %>">
          <a href="<%= root_url %>" style="<%= email_footer_link_style %>">Accueil</a> |
          <a href="<%= root_url %>profile" style="<%= email_footer_link_style %>">Mon Profil</a> |
          <a href="mailto:<%= @support_email %>" style="<%= email_footer_link_style %>">Support</a>
        </p>
        <p style="font-size: 12px; color: rgba(255, 255, 255, 0.6);">
          Notification de sécurité envoyée à <%= @user.email %>
        </p>
      </div>

    </div>
  </div>
</body>
</html>

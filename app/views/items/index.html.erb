

<h1 class="text-2xl font-bold mb-6">Nos offres miaougnifiques</h1>

<div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
  <% @items.each do |item| %>
    <div class="border rounded-lg shadow p-4">
      <%= image_tag item.image_url, class: "w-full h-auto mb-4 rounded" %>
      <h2 class="text-lg font-semibold"><%= item.title %></h2>
      <p class="text-gray-700 mb-2"><%= number_to_currency(item.price) %></p>
      <%= link_to "voir en détails", item_path(item), class: "btn-primary btn-outline-primary text-blue-500 underline" %>
    </div>
  <% end %>
</div>

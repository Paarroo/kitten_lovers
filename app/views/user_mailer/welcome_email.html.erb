<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bienvenue chez <%= @app_name %></title>
</head>
<body>
  <div style="<%= email_wrapper_style %>">
    <div style="<%= email_container_style %>">

      <!-- Header Section -->
      <div style="<%= email_header_style('welcome') %>">
        <div style="<%= email_logo_style %>">🐱</div>
        <h1 style="<%= email_header_title_style %>">Bienvenue chez <%= @app_name %> !</h1>
        <p style="<%= email_header_subtitle_style %>">Votre aventure féline commence maintenant</p>
      </div>

      <!-- Main Content -->
      <div style="<%= email_content_style %>">
        <h2 style="<%= email_content_title_style %>">
          Salut <%= @user.first_name || @user.email.split('@').first %> !
        </h2>

        <p style="<%= email_content_text_style %>">
          <strong>Nous sommes ravis de vous accueillir dans notre communauté d'amoureux des chats ! 🎉</strong>
        </p>

        <p style="<%= email_content_text_style %>">
          Votre compte a été créé avec succès. Vous pouvez maintenant :
        </p>

        <ul style="<%= email_list_style %>">
          <li>Explorer notre galerie de photos de chats adorables</li>
          <li>Ajouter vos photos préférées à votre panier</li>
          <li>Effectuer des achats sécurisés</li>
          <li>Gérer votre profil et vos commandes</li>
        </ul>

        <!-- Call to Action Button -->
        <div style="text-align: center; margin: 30px 0;">
          <a href="<%= @login_url %>" style="<%= email_button_style('success') %>">
            Commencer l'exploration 🐾
          </a>
        </div>

        <!-- Welcome Bonus Box -->
        <div style="<%= email_info_box_style('success') %>">
          <h4 style="margin: 0 0 10px 0; font-size: 18px;">
            🎁 Bonus de bienvenue
          </h4>
          <p style="margin: 0;">
            En tant que nouveau membre, profitez de notre sélection spéciale de photos gratuites !
            Connectez-vous pour découvrir votre cadeau de bienvenue.
          </p>
        </div>

        <p style="<%= email_content_text_style %>">
          Si vous avez des questions, notre équipe support est là pour vous aider à
          <a href="mailto:support@kittenlovers.com" style="<%= email_link_style %>">support@kittenlovers.com</a>
        </p>

        <p style="<%= email_content_text_style %>">
          Miaulement vôtre,<br>
          <strong>L'équipe <%= @app_name %></strong> 🐱
        </p>
      </div>

      <!-- Footer -->
      <div style="<%= email_footer_style %>">
        <p style="<%= email_footer_text_style %>">
          <strong><%= @app_name %></strong> - La plus belle collection de photos de chats
        </p>
        <p style="<%= email_footer_text_style %>">
          <a href="<%= root_url %>" style="<%= email_footer_link_style %>">Accueil</a> |
          <a href="<%= root_url %>about" style="<%= email_footer_link_style %>">À propos</a> |
          <a href="<%= root_url %>contact" style="<%= email_footer_link_style %>">Contact</a> |
          <a href="<%= root_url %>privacy" style="<%= email_footer_link_style %>">Confidentialité</a>
        </p>
        <p style="font-size: 12px; color: rgba(255, 255, 255, 0.6);">
          Cet email a été envoyé à <%= @user.email %>
        </p>
      </div>

    </div>
  </div>
</body>
</html>

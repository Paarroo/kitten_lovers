<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <!-- Brand/Logo -->
    <%= link_to root_path, class: "navbar-brand" do %>
      <%= image_tag "logo.png", alt: "Logo", class: "d-inline-block align-text-top", style: "height: 30px;" %>
    <% end %>

    <!-- Bouton hamburger pour mobile (responsive) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Menu de navigation -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Accueil", root_path, class: "nav-link #{'active' if current_page?(root_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Index", photos_path, class: "nav-link #{'active' if current_page?(photos_path)}" %>
        </li>
        <li class="nav-item">
          <%= link_to "Faq", faq_path, class: "nav-link #{'active' if current_page?(faq_path)}" %>
        </li>
      </ul>

      <!-- Boutons à droite -->
      <div class="d-flex">
        <%= link_to "Connexion", new_user_session_path, class: "btn btn-outline-primary me-2" %>
        <%= link_to "S'inscrire", new_user_registration_path, class: "btn btn-primary" %>
      </div>
    </div>
  </div>
</nav>

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Débloquez votre compte - <%= @app_name %></title>
</head>
<body>
  <div style="<%= email_wrapper_style %>">
    <div style="<%= email_container_style %>">

      <!-- Header Section -->
      <div style="<%= email_header_style('warning') %>">
        <div style="<%= email_logo_style %>">🔒</div>
        <h1 style="<%= email_header_title_style %>">Compte temporairement bloqué</h1>
        <p style="<%= email_header_subtitle_style %>">Débloquez votre accès à <%= @app_name %></p>
      </div>

      <!-- Main Content -->
      <div style="<%= email_content_style %>">
        <h2 style="<%= email_content_title_style %>">
          Bonjour <%= @user.first_name || @user.email.split('@').first %>,
        </h2>

        <p style="<%= email_content_text_style %>">
          Votre compte <%= @app_name %> a été temporairement bloqué suite à plusieurs tentatives de connexion échouées.
        </p>

        <p style="<%= email_content_text_style %>">
          <strong>Cette mesure de sécurité protège votre compte contre les accès non autorisés.</strong>
        </p>

        <!-- Unlock Button -->
        <div style="text-align: center; margin: 30px 0;">
          <a href="<%= @unlock_url %>" style="<%= email_button_style('warning') %>">
            Débloquer mon compte 🔓
          </a>
        </div>

        <!-- Alternative Link -->
        <p style="font-size: 14px; color: #6c757d; text-align: center;">
          Ou copiez ce lien dans votre navigateur :<br>
          <a href="<%= @unlock_url %>" style="<%= email_link_style %>; word-break: break-all;">
            <%= @unlock_url %>
          </a>
        </p>

        <!-- Security Info -->
        <div style="<%= email_info_box_style('warning') %>">
          <h4 style="margin: 0 0 10px 0; font-size: 18px;">
            🔐 Pourquoi ce blocage ?
          </h4>
          <p style="margin: 0;">
            Après plusieurs tentatives de connexion avec un mot de passe incorrect,
            nous avons automatiquement bloqué votre compte pour prévenir tout accès frauduleux.
          </p>
        </div>

        <!-- Security Steps -->
        <div style="<%= email_security_notice_style %>">
          <h4 style="<%= email_security_title_style %>">
            🛡️ Étapes recommandées
          </h4>
          <ol style="<%= email_security_text_style %>; margin: 10px 0 0 0; padding-left: 20px;">
            <li>Cliquez sur le bouton de débloquage ci-dessus</li>
            <li>Connectez-vous avec votre mot de passe habituel</li>
            <li>Si vous avez oublié votre mot de passe, demandez une réinitialisation</li>
            <li>Vérifiez que personne d'autre n'a accès à votre compte</li>
          </ol>
        </div>

        <!-- Suspicious Activity Warning -->
        <div style="<%= email_info_box_style('danger') %>">
          <h4 style="margin: 0 0 10px 0; font-size: 18px;">
            ⚠️ Activité suspecte ?
          </h4>
          <p style="margin: 0;">
            Si vous n'êtes pas à l'origine de ces tentatives de connexion,
            votre compte pourrait être compromis. Contactez immédiatement notre équipe sécurité à
            <a href="mailto:security@kittenlovers.com" style="color: #721c24; font-weight: bold;">
              security@kittenlovers.com
            </a>
          </p>
        </div>

        <p style="<%= email_content_text_style %>">
          Une fois débloqué, nous vous recommandons de :
        </p>

        <ul style="<%= email_list_style %>">
          <li>Changer votre mot de passe si nécessaire</li>
          <li>Vérifier l'activité récente de votre compte</li>
          <li>Vous assurer que vos informations sont à jour</li>
          <li>Activer l'authentification à deux facteurs (bientôt disponible)</li>
        </ul>

        <p style="<%= email_content_text_style %>">
          Nous nous excusons pour ce désagrément et restons à votre disposition pour toute question.
        </p>

        <p style="<%= email_content_text_style %>">
          Sécurisement,<br>
          <strong>L'équipe sécurité <%= @app_name %></strong> 🔒
        </p>
      </div>

      <!-- Footer -->
      <div style="<%= email_footer_style %>">
        <p style="<%= email_footer_text_style %>">
          <strong><%= @app_name %></strong> - Sécurité et protection des données
        </p>
        <p style="<%= email_footer_text_style %>">
          <a href="<%= root_url %>" style="<%= email_footer_link_style %>">Accueil</a> |
          <a href="<%= root_url %>security" style="<%= email_footer_link_style %>">Sécurité</a> |
          <a href="mailto:security@kittenlovers.com" style="<%= email_footer_link_style %>">Support Sécurité</a>
        </p>
        <p style="font-size: 12px; color: rgba(255, 255, 255, 0.6);">
          Notification de sécurité envoyée à <%= @user.email %>
        </p>
      </div>

    </div>
  </div>
</body>
</html>
